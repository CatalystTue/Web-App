## 1. Land living specs

- [x] 1.1 Sync ADDED deltas into `openspec/specs/<capability>/spec.md` for all seven capabilities and verify `openspec validate --specs` succeeds
- [x] 1.2 Archive `document-current-behavior` and verify it lives under `openspec/changes/archive/`
- [x] 1.3 Delete `.kiro/specs` and verify no remaining `.kiro` tree
